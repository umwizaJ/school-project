apiVersion: v1
kind: Service
metadata:
  name: {{ include "school-project.fullname" . }}
  labels:
    {{- include "school-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "school-project.selectorLabels" . | nindent 4 }}
