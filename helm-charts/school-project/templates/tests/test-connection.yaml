apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "school-project.fullname" . }}-test-connection"
  labels:
    {{- include "school-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "school-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
